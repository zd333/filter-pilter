import * as all from '../generated/typings';

export * from '../generated/typings';
/**
 * !Pure types/interfaces only (without any associated code and metadata).
 * !Will be completely erased from comlipled JS code.
 * !Use these in client code to reduce bundle size.
 */
export type { all as PureInterfaces };
